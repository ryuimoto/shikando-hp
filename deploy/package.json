{
  "name": "shikando-hp",
  "version": "1.0.0",
  "private": true,
  "description": "士観道（しかんどう）- 陰陽五行・八字 / タローデパリ 占いサイト",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "node build.mjs && cd public && npx vercel --prod",
    "preview": "node build.mjs && cd public && npx vercel"
  }
}
